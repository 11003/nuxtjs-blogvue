const getters = {
  config: state => state.app.config,
  links: state => state.app.links,
  articlePass: state => state.app.articlePass,
  articleByAbout: state => state.app.articleByAbout,
};
export default getters;
